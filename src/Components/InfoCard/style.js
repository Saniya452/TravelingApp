import { StyleSheet } from "react-native";

const style = StyleSheet.create({
  container: {
    flexDirection: "row",
    alignItems: "center",
    marginBottom: 10,
  },
  icon: {
    width: 40,
    height: 40,
  },
  title: {
    color: "#000",
    fontSize: 13,
    marginHorizontal: 8,
    //padding: 8
  },
});

export default style;
